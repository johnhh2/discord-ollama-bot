[tool.ruff]
# CI runs: ruff check --select F401,F811 src/ tests/
# Project-wide settings live here so contributors and CI agree.

[tool.ruff.lint.per-file-ignores]
# src/__init__.py is a re-export module for tests (`import src as bot`).
# Every import here is intentionally unused by __init__.py itself.
"src/__init__.py" = ["F401"]
